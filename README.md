# 3D In‑Vitro BD Web Agent

This tool identifies, enriches, and ranks high‑probability buyers for
3D in‑vitro and safety assessment technologies.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```